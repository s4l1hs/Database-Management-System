# To insert data, you must edit the CSV file you will use according to these rules.

# Headers must be exactly the same as the database columns.
# Data types must be consistent.
# Empty data can be specified as NA/NULL.
# Use English characters.
# Foreign key values ​​must be correct.
# Don't use single quotes (''), otherwise it will be passed as strings.